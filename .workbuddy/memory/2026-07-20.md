## 2026-07-20

### 数据集选定
- 经过搜索对比（ExpressiveSpeech, BESST, RECOLA, ATC Fatigue Corpus, SUSAS 等 6 个候选），确定数据策略：
  - **主训练**：ATC 管制员疲劳语音库（Shen/NUAA, IEEE Dataport, 700k段真实空管疲劳标注）
  - **辅助验证**：ExpressiveSpeech（DeEAR arousal 交叉验证）
- 约束：纯语音判断，排除 BESST（ECG/EDA）、RECOLA（视频/生理信号）等多模态数据集

### 文档更新（v1.4 → v1.5）
- README.md：新增 6.2 数据集策略，更新技术路线和文件状态表
- docs/一期MVP实现方案.md：第三节数据集策略完全重写，H1/H2/H3 验证方法对齐 ATC Fatigue Corpus，模型改为 XGBoost
- docs/维度回归模块-TCN设计.md：训练数据 NaturalVoices EVC → ATC Fatigue Corpus + ExpressiveSpeech，更新验证目标、风险、资源估算
- docs/系统架构与选型方案.md：MVP 阶段数据集引用更新
- figures/一期MVP模块架构图.svg → v1.4：数据源标注更新
- figures/系统架构图.svg → v1.4：数据+模型标注更新
- figures/工作状态分析实现流程图.svg → v1.4：XGBoost+SHAP + SHAP溯源

### step4 模块编码
- 创建 `step4_regression/` 目录，含 4 个文件：
  - `__init__.py`：包入口，导出 train_models / predict / explain_segment / train_from_step3 / predict_from_step3
  - `model.py`：XGBoost 双任务训练（train_models）+ 推理（predict）+ SHAP 解释（explain_segment）+ 模型持久化（load_models）+ CCC 评估 + exertion 代理标签构造
  - `dataset.py`：ATC Fatigue Corpus 加载器（scan 目录→step1-3全链路→z-scored DataFrame）+ ExpressiveSpeech 加载 + leave-one-speaker-out 拆分
  - `pipeline.py`：train_from_step3 / predict_from_step3（支持 DataFrame 或音频文件输入）+ cross_validate（LOSO 交叉验证）
- 接口对齐 step3 输出（z-scored DataFrame），风格与 step1-3 一致
- 依赖：xgboost + shap（延迟导入，安装时才检查）
- 重命名 `维度回归模块-TCN设计.md` → `维度回归模块-XGBoost设计.md`，同步更新 README 和 MVP 方案中的引用
